[package]
name = "fd-design-system"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Fusion-Design 设计系统 — 三套内置规范 + Token 管理"

[lib]
name = "fd_design_system"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
# A-3：CSS 值净化共享真相源（to_css_value/to_css_custom_properties）。
fd-canvas-core = { path = "../fd-canvas-core" }
# op-design-lint = { workspace = true }  # subtree 激活后启用
