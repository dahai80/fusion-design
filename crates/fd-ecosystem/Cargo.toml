[package]
name = "fd-ecosystem"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Fusion-Design 生态联动 — 对接 Fusion Code/Simulation/KB/CLI"

[lib]
name = "fd_ecosystem"
path = "src/lib.rs"

# Callers: fd-cli (export/generate IPC send, train), DesignBridge.swift
# Affected API: EcosystemLink::send/list/consume/sync_to_code/search_templates/templates,
#   TrainerClient::run_sft/run_rlsl
# H-A3/P2-2：watch_async + 自建 MCP 层已移除（零外部调用，死代码）。
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
# H-A3/P2-2：notify 已移除（仅 watch_async 用，已删）。
fd-canvas-core = { path = "../fd-canvas-core" }
fd-codegen = { path = "../fd-codegen" }
# op-mcp 未引入私有依赖；自建 MCP 层已移除（不合规，见审计 H-A4）。

[dev-dependencies]
tempfile = "3"
