# 变更说明 v0.1.6（补丁）

## 处置

- **Issue #11**（DEFAULT_MLX_ENDPOINT 11434→11432 经 gateway）：**落地**。用户 2026-08-07 裁定方案A 否决，回退 netlayer-compliance-plan.md 方案B，所有下游统一经 fusion-gateway `:11432`。
- **PR**：无开放 PR。
- **分支**：仅 `main`，无需合并。

## 变更

统一网关迁移（方案B），fusion-design 不再直连 fusion-mlx 11434：

- `fd-ai-adapter/src/lib.rs`：`DEFAULT_MLX_ENDPOINT` `11434` → `11432`；方案A 注释改为方案B 说明（标注历史已否决）。
- `fd-cli/src/main.rs`：4 处 `--endpoint` `default_value` `11434` → `11432`。
- `CLAUDE.md`：端口与方案说明同步更新。
- 保留 `FUSION_MLX_BASE_URL` / `--endpoint` 覆盖能力（显式指回 11434 仍可）。
- gateway 鉴权：`FUSION_MLX_API_KEY` 设为 gateway key（如 master_key）。

## 实测核验

- fusion-gateway 监听 `*:11432`，`/health` 200，`/v1/chat/completions` 带 master_key 实测 HTTP 200，返回正常 `chat.completion`。
- fusion-mlx 本体仍监听 11434（未动）。

## 验收（issue #11）

- [x] 默认配置 fd-ai-adapter / fd-cli 调 MLX 经 gateway 200（E2E：`fd-cli generate` 模型返回 "ok"）
- [x] grep `11434` 仅剩注释（标注已废弃/历史）
- [x] 无 "严禁默认值写 11432" 字样

## 验证

- `cargo fmt --all -- --check`：clean
- `cargo clippy --workspace --all-targets`：0 warning / 0 error
- `cargo test --workspace`：272 passed, 0 failed

## 版本

0.1.5 → 0.1.6（全部 crate 继承 `version.workspace = true`）。
