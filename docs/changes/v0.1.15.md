# v0.1.15 — 发布件刷新：闭合 post-v0.1.14 三项 P3 真缺口

> 日期：2026-08-30
> 上版：v0.1.14（tagged release @ f3f0597，492 tests）
> 本版工作树：495 tests / 0 failed。
> 审计基线：`~/fusion/audit/fusion-design-audit-result-product-0830.md`（91/100 Strong）

## 背景

v0.1.14 正式发版（tag v0.1.14 @ f3f0597）后，main 连续合入三个 PR：

- PR #26（07e39c0）：闭合 0829 审计 3 项「留 TODO」——ARCH-10 round-3（drag thread_local 迁 events.rs）、FAULT-1/ARCH-13 单测（SSE stall 超时 + CanvasError 死变体）、OPS-7 签名真验（失败致命化 + 签后 codesign/spctl 断言）。
- PR #27（e47e80f）：issue #20，fd-cli `chat` 新增 `--format sse`，统一 studio 流式协议。
- PR #28（bbad81d）：闭合 post-v0.1.14 三项 P3 真缺口——FUNC-9（SVG+PDF text 旋转补全）、PERF-4（PDF 逐页流式降内存峰值）、ARCH-10 r4（design-lint + fd-export + fd-codegen 三 god-file 拆完）。

tag v0.1.14 @ f3f0597 落后 main @ bbad81d 三个 PR，**published tarball 缺上述 4 项修复**（#26/#27/#28）。本版打新 tag v0.1.15 刷新发布件，让 binary 含最新修复。

## 本版变更

- **FUNC-9**（PR #28，e114bee）：SVG + PDF text arm 渲染漏 `node.rotation` → 两路补 transform/text matrix 旋转，对齐 HTML/React/SwiftUI 五维。
- **PERF-4**（PR #28，ecf2e4d）：`from_pen_document` 一次性 collect 全 page 结果到 Vec → 改逐页流式，降大文档 PDF 内存峰值。
- **ARCH-10 r4**（PR #28，e398a65+e26bee8+ad4fec6）：三 god-file 全拆完——design-lint→color/detectors/fix，fd-export→html/svg/png/pdf/util，fd-codegen（1600→888）→html/react/swiftui/util。纯位置迁移零行为变更。lib.rs 无 >1000 行 god-file 残留。
- **ARCH-10 round-3**（PR #26，07e39c0）：host-web drag thread_local（ACTIVE_DRAG_MOVE/PENDING_DRAG_UP/RESIZE_HANDLES）+ DragMoveClosure + cleanup_pending_drag 迁 events.rs，lib.rs 反向 use。零逻辑变更。
- **FAULT-1 单测**（PR #26）：SSE stall 超时单测——`tokio::time::timeout` 包裹 stream.next()，建连后无 chunk 卡死不无限挂起。
- **ARCH-13 单测**（PR #26）：CanvasError 死变体 NodeNotFound/PageNotFound 直构造断言 Display + downcast。
- **OPS-7 签名真验**（PR #26）：build.sh 签名失败致命化（exit 1）+ 签后 `codesign --verify --strict` + `spctl --assess` 真验断言。无 secret 维持 warn+skip 出未签名件。
- **issue #20 SSE**（PR #27，e47e80f）：fd-cli `chat` 新增 `--format sse`（raw OpenAI text/event-stream）+ 保留 `--format ndjson`。studio 解析器零改动可消费。

## 退化与留 TODO

无。PERF-3 / ARCH-3 / OPS-18 经 0830 审计核实**已闭合或非缺口**：

- **PERF-3 大文档虚拟化**：DOM 渲染路径 `render_dom.rs:230 viewport_cull_update` 已实现视口虚拟化（AABB 相交 + zoom/pan + 离屏 remove + 入屏 fragment 批量加），canvas 路径死代码。`validate_limits` 兜底 MAX_NODE_TOTAL=100k / MAX_NODE_DEPTH=64。非 niche 待修，实为已落地。
- **ARCH-3 fd-cli 委托**：已拆 `commands/*.rs`（17 模块），main.rs `run()` 标准 clap-derive dispatch。非单行 god-function。
- **OPS-18 cargo-audit 离线**：advisory DB 是网络资源无离线副本，CI `cargo audit` 阻断（ci.yml:104）是唯一正确权威，固定旧 DB 反漏新通告有害。物理不可能，非工程缺口。

## 验证

- `cargo test --workspace`：495 passed, 0 failed, 3 ignored。
- `cargo clippy --workspace --all-targets -- -D warnings`：clean。
- `cargo fmt --all -- --check`：clean。
- 三道 deny 门禁（deny-panic / deny-unwrap-expect / deny-external-http）：clean。
- 发布件本地构建：`Scripts/build.sh` → `fusion-design-0.1.15-aarch64-apple-darwin.tar.gz`（4.4M）+ sha256。binary `--version` 报 0.1.15，wasm-bindgen 0.2.126 产物 _bg.wasm + .js 齐全。
