# 变更说明 v0.1.5（补丁）

## 处置

- **Issue #5**（`DEFAULT_MLX_ENDPOINT` → :11432）：**关闭**。经核验 fusion-mlx 实际监听 11434，11432 是 fusion-gateway。改默认值会与方案A「直连 fusion-mlx」冲突；可配置性（要求3）已由 `FUSION_MLX_BASE_URL` + CLI `--mlx-endpoint` 满足。跨仓库 netlayer 迁移属上游协调。
- **PR**：无开放 PR。
- **分支**：仅 `main`，无需合并。

## Lint 全绿

清理工作区全部 clippy 警告（0 warning / 0 error）：

- `fd-host-web`：`vec![]` → 数组字面量、`&format!` 去多余借用、`render_depth_limit_constant_bounded` 改编译期 `const _: ()` 校验（消除「常量断言」警告）。
- `fd-canvas-core`：2 处 `NodeStyle::default()` + 字段赋值 → 结构体初始化器（`field_reassign_with_default`）。
- `fd-design-lint`：2 处移除未用 `FlexDirection` import。

## 验证

- `cargo fmt --all -- --check`：clean
- `cargo clippy --workspace --all-targets`：0 warning, 0 error
- `cargo test --workspace`：272 passed, 0 failed

## 版本

0.1.4 → 0.1.5（全部 crate 继承 `version.workspace = true`）。
