# v0.1.16-rc.1 — 新发布周期 release candidate

> 日期：2026-08-30
> 上版：v0.1.15（tagged release @ 1c75e06，495 tests）
> 本版工作树：495 tests / 0 failed。
> 性质：prerelease（RC），新发布周期起点。

## 背景

v0.1.15 正式商用发版（tag v0.1.15 @ 1c75e06 = main，GitHub Release Latest）已落地，生产审计 94/100 Strong，零阻断、零待办。无新增代码变更。本版打新周期 release candidate v0.1.16-rc.1：版本号 bump，binary 内容与 v0.1.15 同 commit 等价（仅版本号 + prerelease 标记差异），作为下一正式版 v0.1.16 的候选基线，便于后续增量变更按 RC 阶梯验证。

## 本版变更

- 版本 bump 0.1.15 → 0.1.16-rc.1：`Cargo.toml` `[workspace.package]` version，13 crate 经 `version.workspace = true` 继承，`Cargo.lock` 同步（13 条版本行变更，零依赖漂移）。
- 无功能代码变更、无退化。binary 内容 = v0.1.15 等价 + 版本号字段。

## 退化与留 TODO

无。继承 v0.1.15 零残留状态——PERF-3 / ARCH-3 / OPS-18 经 0830 审计核实已闭合或非缺口（详见 `docs/changes/v0.1.15.md`）。

## 验证

- `cargo test --workspace`：495 passed, 0 failed, 3 ignored。
- `cargo clippy --workspace --all-targets -- -D warnings`：clean。
- `cargo fmt --all -- --check`：clean。
- 三道 deny 门禁（deny-panic / deny-unwrap-expect / deny-external-http）：clean。
